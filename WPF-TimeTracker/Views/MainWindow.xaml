<Window x:Class="WPF_TimeTracker.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Time Tracker" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel xmlns:vm="clr-namespace:WPF_TimeTracker.ViewModels" />
    </Window.DataContext>
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="File">
                    <MenuItem Header="Exit" Click="Exit_Click"/>
                </MenuItem>
                <MenuItem Header="View">
                    <MenuItem Header="Timers" Click="Timers_Click"/>
                    <MenuItem Header="Categories" Click="Categories_Click"/>
                    <MenuItem Header="Reports" Click="Reports_Click"/>
                </MenuItem>
            </Menu>
            <Frame Name="MainFrame" NavigationUIVisibility="Hidden"/>
        </DockPanel>
    </Grid>
</Window>
